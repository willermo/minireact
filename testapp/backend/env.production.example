# INSTRUCTIONS:
# 1. Edit the values below as needed for production
# 2. Save this file
# 3. Rename it to '.env.production' by adding a leading dot and removing '.example'
#    Example: 'cp env.production.example .env.production'

# Production environment settings

# Frontend URL for production (update with your production domain)
FRONTEND_NGINX_URL=<your_production_domain - eg: https://your-production-domain.com or https://localhost:4443>

# Cookie names for production
JWT_COOKIE_NAME=<your_jwt_cookie_name - eg: authToken>
CSRF_COOKIE_NAME=<your_csrf_cookie_name - eg: csrfToken>
REFRESH_TOKEN_COOKIE_NAME=<your_refresh_token_cookie_name - eg: refreshToken>